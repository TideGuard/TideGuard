# Copy to `.dev.vars` for local development (`npm run setup` does this).
# Generate a secret: https://tideguard.dev/token  or  openssl rand -hex 32
# Deploy-to-Cloudflare prompts for this value — leave empty here so the field starts blank.
TOKEN_SECRET=
